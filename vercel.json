{
  "github": {
    "silent": true
  },
  "buildCommand": "cp .env.build .env.local && cd apps/nextjs && CI=1 bun run build",
  "installCommand": "bun install",
  "outputDirectory": "apps/nextjs/.next",
  "framework": "nextjs"
}
